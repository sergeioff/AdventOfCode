package `2025`.day01

import munit.FunSuite

// TODO: should be fixed
class Tests extends FunSuite:
//   test("test 1"):
//     assertEquals(turn(50, parseTurn("L68")), 82)

//   test("normalizeRezult"):
//     println(normalizeResult(100))
//     println("aaa")

  // test("rotateV2"):
  //   assertEquals(rotate(0, subtract, 5), Result(result = 95, numberOfZeroClicks = 0))
  //   assertEquals(rotate(52, sum, 48), Result(result = 0, numberOfZeroClicks = 1))
  //   assertEquals(rotate(55, subtract, 55), Result(result = 0, numberOfZeroClicks = 1))
  //   assertEquals(rotate(0, subtract, 0), Result(result = 0, numberOfZeroClicks = 0))
  //   assertEquals(rotate(5, sum, 5), Result(result = 10, numberOfZeroClicks = 0))
  //   assertEquals(rotate(50, subtract, 68), Result(result = 82, numberOfZeroClicks = 1))
  //   assertEquals(rotate(7, sum, 263), Result(result = 70, numberOfZeroClicks = 2))
  //   assertEquals(rotate(50, sum, 1000), Result(result = 50, numberOfZeroClicks = 10))
  //   assertEquals(rotate(0, sum, 100), Result(result = 0, numberOfZeroClicks = 1))
  //   assertEquals(rotate(0, sum, 1000), Result(result = 0, numberOfZeroClicks = 10))
  //   assertEquals(rotate(0, subtract, 100), Result(result = 0, numberOfZeroClicks = 1))
  //   assertEquals(rotate(0, subtract, 1000), Result(result = 0, numberOfZeroClicks = 10))
    


/* def normalizeResult(result: Int, level: Int = 0): Result =
  if (result == 100) return Result(0, level + 1)
  if (result >= leftBorder && result <= rightBorder)
    return Result(result, level)
  else if (result < leftBorder)
    return normalizeResult(result + zeroMatch, level + 1)
  else
    return normalizeResult(result - zeroMatch, level + 1) */